// Constant for API base URL
export const API_BASE_URL = 'http://192.168.57.86:8000/api/v1/'; 

//change the IP address to your local IP
//To check this address, run the command `ipconfig` on Windows
//Then look for the IPv4 Address under the Wireless LAN adapter Wi-Fi section

//To make sure the API is running, run the command `php artisan serve --host=0.0.0.0`
//this makes sure the API is accessible from other devices on the same network